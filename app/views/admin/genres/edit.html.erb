<div class="container">
  <div class="row">
    <div class="col-md-10 offset-md-1">
      <h2>ジャンル編集</h2>
      <div class="form_group">
         <%= form_for @genre, :url => {:action => :update} do |f| %>
          <%= f.label :"ジャンル名" %>
          <%= f.text_field :name  %>
          <%= f.submit '変更を保存' %>
         <%= link_to "Destroy", admin_genre_path(@genre), class:"btn btn-danger", method: :delete, "data-confirm" => "本当に削除しますか?" %></td>
        <% end %>
      </div>
    </div>

  </div>
